name: posts_api_example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^7.0.0
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  http: ^0.13.1
  shimmer: ^2.0.0

dev_dependencies:
  build_runner: ^2.1.4
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.1

flutter:
  uses-material-design: true
